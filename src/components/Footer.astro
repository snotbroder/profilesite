---
import "../css/general.css";
---

<section>
  <div class="flex">
    <a href="/"><h4>#index</h4></a>
    <a href="/projects"><h4>#latest projects</h4></a>
    <a href="/about"><h4>#about</h4></a>
  </div>
  <h2>&copy;joris ten voorde</h2>
</section>

<style>
  section {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 100%;
    height: 30rem;
    background: var(--gradient1);

    padding: 3rem 0 0 6rem;
    /* overflow-y: hidden; */
    position: relative;
    margin-top: var(--space-m);

    &::after {
      display: block;
      content: "";
      background: url(/dog.webp);
      background-size: cover;
      transform: scalex(-1);
      background-repeat: no-repeat;
      width: 40%;
      height: 80%;
      aspect-ratio: 1/1;
      position: absolute;
      top: -15rem;
      left: 2%;

      z-index: -1;
    }
  }
  .flex {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
  h2 {
    text-align: end;
    color: transparent;
    -webkit-text-stroke: var(--accent2) 3px;

    margin: 0;
    padding: 0;
  }
  h4 {
    color: var(--accent2);
  }

  .arrow {
    color: var(--primary);
  }

  @media (min-width: 900px) {
    section {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      width: 100%;
      height: 20rem;
    }

    .flex {
      margin-left: auto;
      display: flex;
      flex-direction: row;
      gap: 4rem;
    }
  }
</style>
